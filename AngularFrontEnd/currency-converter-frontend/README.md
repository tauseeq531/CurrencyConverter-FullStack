
# Currency Converter Frontend (Angular 17 + Material + Tailwind)

## Setup
```bash
cd currency-converter-frontend
npm install
npm start
```
The app expects the backend at `https://localhost:5001/api`. Update `src/environments/environment.ts` if needed.

## Features
- Converter form with validation & loading state
- History table
- Dark mode toggle
- Basic error interceptor
